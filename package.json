{
  "name": "stt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "install:webgpu-runtime": "npm install",
    "verify:webgpu-runtime": "node --input-type=module -e \"await import('@huggingface/transformers'); await import('@huggingface/tokenizers'); await import('onnxruntime-node')\""
  },
  "dependencies": {
    "@huggingface/tokenizers": "^0.1.3",
    "@huggingface/transformers": "^4.1.0",
    "onnxruntime-node": "^1.24.3"
  }
}
